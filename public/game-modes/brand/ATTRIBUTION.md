# Brand logo source

The SVG files in `source/` are normalized from WorldVectorLogo CDN sources, with
IKEA recreated as a transparent vector for consistent in-game masking. The
generated PNG and mask files in `generated/` are derived build assets.

Brand names and marks remain trademarks of their respective owners; verify
permission requirements before commercial use.
